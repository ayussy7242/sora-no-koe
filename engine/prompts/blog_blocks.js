"use strict";

const BLOG_BLOCKS_USER_GUIDE = `
以下のINPUTブロックから、HTMLのみでブログ本文を生成する。

出力ルール:
- 出力はHTMLのみ。Markdown禁止。
- ブロックの順序はINPUTの順序を厳守。
- 各ブロックは必ず <h2>タイトル</h2> で始める。
- 各ブロックの最初の段落は FACTS を1〜3文で要約して書く。
- ITEMS_AS_H3: yes のブロックは、ITEMSを順番に <h3> で1件ずつ書く。
- 箇条書き禁止。短文連発禁止。必ず述語で閉じる。
- 本文で「あなた」禁止。
- 占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
- 因果の講義にしない。
- 「示す/意味する/促す/影響する/課題/可能性」で閉じない。
- まとめは書かない。最後は余韻で閉じる。

主語ルール（最重要）:
- 主語は構造（配置/角度/組み合わせ/回路/層/圧/方向/切り替え/残り方）。
- 人を主語にして結論を置かない。

語りの方針:
- 事実は先に置き、その後に“圧・温度・残り方”として描写する。
- 「入り方の順番」「視点の位置」「圧の向き」「残り方」を書く。
- 断片列挙にしない。各文は文として閉じる。
- 章ごとの文量は2〜5段落を目安にする（短すぎ禁止）。
`.trim();

const BLOG_BLOCK_SECTION_GUIDE = `
以下のINPUTブロックから、単一ブロックのHTMLを生成する。

出力ルール:
- 出力はHTMLのみ。Markdown禁止。
- <h2>タイトル</h2> から始める。
- FACTSを1〜3文で要約した短い段落を続ける。
- 箇条書き禁止。短文連発禁止。必ず述語で閉じる。
- 本文で「あなた」禁止。
- 占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
- 因果の講義にしない。
- 「示す/意味する/促す/影響する/課題/可能性」で閉じない。

主語ルール:
- 主語は構造（配置/角度/組み合わせ/回路/層/圧/方向/切り替え/残り方）。
- 人を主語にして結論を置かない。
`.trim();

const BLOG_BLOCK_ITEM_GUIDE = `
以下のINPUTから、単一アイテムのHTMLを生成する。

出力ルール:
- 出力はHTMLのみ。Markdown禁止。
- <h3>タイトル</h3> を必ず先頭に置く（タイトルはINPUTの指定どおり）。
- 続く段落は2〜4文。必ず述語で閉じる。
- 箇条書き禁止。短文連発禁止。
- 本文で「あなた」禁止。
- 占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
- 因果の講義にしない。
- 「示す/意味する/促す/影響する/課題/可能性」で閉じない。

主語ルール:
- 主語は構造（配置/角度/組み合わせ/回路/層/圧/方向/切り替え/残り方）。
- 人を主語にして結論を置かない。
`.trim();

module.exports = Object.freeze({
  BLOG_BLOCKS_USER_GUIDE,
  BLOG_BLOCK_SECTION_GUIDE,
  BLOG_BLOCK_ITEM_GUIDE,
  BLOG_STRUCT_HOUSE_GUIDE: `
以下のHOUSE情報から、短い構造記述を1〜2文で書く。

出力ルール:
- 出力は日本語の本文のみ（HTMLタグ不要）。
- 80〜120字を目安にする。
- 占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
- 因果の講義にしない。
- 「示す/意味する/促す/影響する/課題/可能性」で締めない。
- 主語は構造（配置/圧/残り方/集中/方向）。
- 人を主語にしない。
`.trim(),
  BLOG_STRUCT_ELEMENT_GUIDE: `
以下の元素/三区分の数から、短い構造コメントを1段落で書く。

出力ルール:
- 出力は日本語の本文のみ（HTMLタグ不要）。
- 60〜120字を目安にする。
- 占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
- 因果の講義にしない。
- 「示す/意味する/促す/影響する/課題/可能性」で締めない。
- 主語は構造（偏り/圧/残り方/温度）。
- 人を主語にしない。
  `.trim(),
  BLOG_LONG_OVERVIEW_GUIDE: `
以下のFACTSから「今日の全体圧（圧縮）」を1段落で書く。
出力は日本語本文のみ（HTMLなし）。
文字数は280〜420字を目安にする。
2〜3段落で、段落間は空行。
3〜4文で短くまとめる。
まとめない／結論を出さない。
占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
主語は構造（配置/角度/層/圧/方向/残り方）。
「示す/意味する/促す/影響する/課題/可能性」で締めない。
語調はやわらかめにする。
`.trim(),
  BLOG_LONG_POSITION_GUIDE: `
以下のFACTSから「天体配置（位置）」の本文を1段落で書く。
出力は日本語本文のみ（HTMLなし）。
文字数は100〜140字を目安にする。
3文で書き、最大でも4文まで。
説明→例示→余韻の順で書く。
角度（アスペクト）には触れない。
サインの質／ハウスの質／度数のニュアンス／逆行だけを書く。
2段落まで、段落間は空行。
占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
主語は構造。
語調はやわらかめにする。
`.trim(),
  BLOG_LONG_RESONANCE_GUIDE: `
以下のFACTSから「共鳴（角度）」の本文を1段落で書く。
出力は日本語本文のみ（HTMLなし）。
文字数は120〜160字を目安にする。
3文で書き、最大でも4文まで。
説明→例示→余韻の順で書く。
2段落まで、段落間は空行。
占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
主語は構造。
語調はやわらかめにする。
`.trim(),
  BLOG_LONG_HOUSE_GUIDE: `
以下のFACTSから「ハウス本文」を1段落で書く。
出力は日本語本文のみ（HTMLなし）。
文字数は90〜140字を目安にする。
3文で書き、最大でも4文まで。
説明→例示→余韻の順で書く。
2段落まで、段落間は空行。
占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
主語は構造。
語調はやわらかめにする。
`.trim(),
  BLOG_LONG_ELEMENTS_GUIDE: `
以下のFACTSから「元素／三区分」の本文を1段落で書く。
出力は日本語本文のみ（HTMLなし）。
文字数は250〜350字を目安にする。
優勢元素の物性→欠損の影→区分との組み合わせ→圧の質感の順で短く書く。
2〜3段落で、段落間は空行。
占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
主語は構造。
語調はやわらかめにする。
`.trim(),
  BLOG_LONG_RETRO_GUIDE: `
以下のFACTSから「逆行本文」を1段落で書く。
出力は日本語本文のみ（HTMLなし）。
文字数は180〜250字を目安にする。
速度の内転→サイン物性→強ハウスとの関係→近接角度との絡みの順で書く。
占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
主語は構造。
語調はやわらかめにする。
`.trim(),
  BLOG_LONG_AFTERTASTE_GUIDE: `
以下のFACTSから「余韻」を1段落で書く。
出力は日本語本文のみ（HTMLなし）。
文字数は180〜260字を目安にする。
重心→強い層→滞留場所の順で短く書く。
2段落まで、段落間は空行。
占い化しない。未来断定/指示/救済/運命固定/絶対語は禁止。
主語は構造。
語調はやわらかめにする。
`.trim(),
});
