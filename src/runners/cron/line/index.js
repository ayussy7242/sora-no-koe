"use strict";

const { runDaily8 } = require("./daily8");
const { rebuildDaily8 } = require("./rebuild");
const { sendDaily8 } = require("./send");

module.exports = {
  runDaily8,
  rebuildDaily8,
  sendDaily8,
};
