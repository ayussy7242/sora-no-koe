"use strict";

/**
 * V2 entrypoint (preferred).
 * Thin wrapper around orchestrator.
 */
const { generateBlueprintLightTextV2 } = require("../orchestrator");

module.exports = {
  generateBlueprintLightTextV2,
};
