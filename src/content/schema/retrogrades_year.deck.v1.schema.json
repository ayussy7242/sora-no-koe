{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "retrogrades_year.deck.v1.schema.json",
  "title": "Retrogrades Year Deck (v1)",
  "type": "object",
  "required": ["type", "version", "layout_key", "slides"],
  "properties": {
    "type": { "const": "carousel_deck" },
    "version": { "const": "v1" },
    "layout_key": { "type": "string", "minLength": 1 },
    "title": { "type": "string" },
    "subtitle": { "type": "string" },
    "badge": { "type": "string" },
    "data": { "type": "object" },
    "slides": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/slide" }
    }
  },
  "definitions": {
    "date_local": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "row": {
      "type": "object",
      "required": ["planet_key", "start_local", "end_local", "sign_key_start", "sign_key_end"],
      "properties": {
        "planet_key": { "type": "string", "minLength": 1 },
        "start_local": { "$ref": "#/definitions/date_local" },
        "end_local": { "$ref": "#/definitions/date_local" },
        "sign_key_start": { "type": "string", "minLength": 1 },
        "sign_key_end": { "type": "string", "minLength": 1 },
        "shadow_start_local": { "oneOf": [ { "$ref": "#/definitions/date_local" }, { "type": "null" } ] },
        "shadow_end_local": { "oneOf": [ { "$ref": "#/definitions/date_local" }, { "type": "null" } ] },
        "note": { "oneOf": [ { "type": "string" }, { "type": "null" } ] }
      },
      "additionalProperties": true
    },
    "slide": {
      "type": "object",
      "required": ["id", "type"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "type": { "type": "string", "minLength": 1 },
        "title": { "type": "string" },
        "tagline": { "type": "string" },
        "badge": { "type": "string" },
        "source": { "type": "string" },
        "rows": {
          "type": "array",
          "items": { "$ref": "#/definitions/row" }
        }
      },
      "additionalProperties": true,
      "allOf": [
        {
          "if": {
            "properties": { "type": { "const": "table" } }
          },
          "then": {
            "required": ["source"],
            "properties": {
              "source": { "enum": ["retrogrades"] }
            }
          }
        }
      ]
    }
  }
}
