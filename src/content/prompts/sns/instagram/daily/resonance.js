"use strict";

const {
  SORA_AI_PUBLIC_IG_COMMON,
  POLITE_TONE_COMMON,
} = require("../common");

/* =================================================
IG｜今日の共鳴（split版 / 昼投稿）
================================================= */

const SORA_AI_USER_GUIDE_IG_RESONANCE_SPLIT = `
Instagram「昼投稿」の共鳴本文を生成する。

目的:
- AとBの関係説明ではなく「同時に存在した結果」を書く
- 抽象語ではなく、行動・状態で書く
- 「で、どうなるの？」に答える

形式:
- 2〜3文
- 2〜3行（1文=1行）
- 80〜100文字目安
- 絵文字は使わない

思考プロセス（必須）:
1) Aが何をしようとしているか（具体）
2) Bが何をしようとしているか（具体）
3) その結果、現実でどういう状態になるか（必ず書く）

出力ルール:
- 抽象語だけで終わらない
- AとBを並べて終わらない
- 結果を必ず書く
- 行動・状態で書く
- 行間に余白を感じるリズムで改行する

禁止:
- 「にじむ」「重なる」「エネルギー」などの抽象語
- 助言 / 心理断定 / 行動示唆 / 運勢表現
- 「あなた」

INPUT:
- ASPECT
- A_BODY / B_BODY
- A_SIGN / B_SIGN
- A_HOUSE / B_HOUSE
- ORB

${SORA_AI_PUBLIC_IG_COMMON}
${POLITE_TONE_COMMON}
`.trim();

/* =================================================
IG｜今日の共鳴（caption）
================================================= */

const SORA_AI_USER_GUIDE_IG_RESONANCE_CAPTION = `
Instagram投稿の共鳴キャプション本文を生成する。

目的:
- カルーセル本文の延長として、共鳴の意味が読み取れる補助線にする
- AとBの関係説明ではなく「同時に存在した結果」を書く
- 具体的な状態・流れとして伝える

形式:
- 3〜5文
- 1文=1行
- 各行のあいだに空行を1つ入れる
- 260〜300文字目安
- 絵文字1つ（文末に控えめに置く）

思考プロセス（必須）:
1) Aが何をしようとしているか（天体×星座）
2) Bが何をしようとしているか（天体×星座）
3) それがどの領域で起きているか（ハウス）
4) その角度がどういう接続をつくっているか（アスペクト）
5) その結果、現実でどういう状態になるか（必ず書く）

【ハウスの扱い】
- A_HOUSE / B_HOUSE は「どの領域で起こりやすいか」の輪郭として使う

出力ルール:
- 抽象語だけで終わらない
- AとBを並べて終わらない
- 結果を必ず書く
- 行動・状態で書く
- 行間に余白を感じるリズムで改行する（空行を挟む）
- 絵文字は文末に1つだけ入れる

禁止:
- 「にじむ」「重なる」「エネルギー」などの抽象語
- 助言 / 心理断定 / 行動示唆 / 運勢表現
- 「あなた」

INPUT:
- ASPECT
- A_BODY / B_BODY
- A_SIGN / B_SIGN
- A_HOUSE / B_HOUSE
- ORB

${SORA_AI_PUBLIC_IG_COMMON}
${POLITE_TONE_COMMON}
`.trim();

module.exports = Object.freeze({
  SORA_AI_USER_GUIDE_IG_RESONANCE_SPLIT,
  SORA_AI_USER_GUIDE_IG_RESONANCE_CAPTION,
});
