"use strict";

const {
  SORA_AI_PUBLIC_IG_COMMON,
  POLITE_TONE_COMMON,
} = require("../common");

/* =================================================
IG｜今日の共鳴（split版 / 昼投稿）
================================================= */

const SORA_AI_USER_GUIDE_IG_RESONANCE_SPLIT = `
Instagram「昼投稿」の共鳴本文を生成する。

目的:
- AとBの関係説明ではなく「同時に存在した結果」を書く
- 抽象語ではなく、行動・状態で書く
- 「で、どうなるの？」に答える

形式:
- 2〜3文
- 2〜3行（1文=1行）
- 80〜100文字目安
- 絵文字は使わない

思考プロセス（必須）:
1) Aが何をしようとしているか（具体）
2) Bが何をしようとしているか（具体）
3) その結果、現実でどういう状態になるか（必ず書く）

出力ルール:
- 抽象語だけで終わらない
- AとBを並べて終わらない
- 結果を必ず書く
- 行動・状態で書く
- 行間に余白を感じるリズムで改行する

禁止:
- 「にじむ」「重なる」「エネルギー」などの抽象語
- 助言 / 心理断定 / 行動示唆 / 運勢表現
- 「あなた」

INPUT:
- ASPECT
- A_BODY / B_BODY
- A_SIGN / B_SIGN
- A_HOUSE / B_HOUSE
- ORB

${SORA_AI_PUBLIC_IG_COMMON}
${POLITE_TONE_COMMON}
`.trim();

/* =================================================
IG｜今日の共鳴（caption）
================================================= */

const SORA_AI_USER_GUIDE_IG_RESONANCE_CAPTION = `
Instagram投稿の共鳴キャプション本文を生成する。

目的:
- カルーセル本文の延長として、共鳴の意味が読み取れる補助線にする
- AとBの関係説明ではなく「同時に存在した結果」を書く
- 具体的な状態・流れとして伝える

形式:
- 4〜7行
- 1文=1行
- 各行のあいだに空行を1つ入れる
- 最低260文字以上
- 260〜360文字に収まるまで書き直す前提で出力する
- 各行の文末に必ず絵文字を1つ入れる
- 絵文字がない行は無効とする
- 絵文字は文末のみ使用する
- 絵文字を入れる場合、その文に「。」はつけない
- 各文の句読点「、」は使用不可
- 文の長さを揃えない
- 短文と中長文を混ぜる
- 1行だけ情報を少し多く持たせたり、リズムを各文章で変える
- 各ステップは必要に応じて2文に分解してよい

文章ルール:
1) Aの動き（天体×星座）の配置と力学について書く

2) Bの動き（天体×星座）の配置と力学について書く

3) それが起きる領域（ハウス）を書く
- どの領域に作用が集まるか
- 何が起きやすい場かを具体的に書く

4) アスペクトによる接続を書く
- AとBがどのように結びつくか
- 動きがどう連結されるか
- 単なる接続ではなく「どう作用するか」を書く

5) 接続によって生まれる状態を書く
- AとBが同時に存在した結果を書く
- 動きがどのように変化したかを書く

6) 状態の具体化を書く（ここ重要）
- 抽象で終わらず、現実としてどう現れるかを書く
- 動き・流れ・変化として書く

7) 最後は「何が起きているか」で着地する

出力ルール:
- 抽象語だけで終わらない
- AとBを並べて終わらない
- 結果を必ず書く
- 行動・状態で書く
- 行間に余白を感じるリズムで改行する（空行を挟む）
- 絵文字は文末に1つだけ入れる
- 文字数が足りない場合は、各行で配置の働き・接続・結果を1段深く具体化して増やす
- 短い言い換えを増やして埋めない
- ハウス・接続・結果の行は特に説明量を持たせる

禁止:
- 「にじむ」「重なる」「エネルギー」などの抽象語
- 助言 / 心理断定 / 行動示唆 / 運勢表現
- 「あなた」

INPUT:
- ASPECT
- A_BODY / B_BODY
- A_SIGN / B_SIGN
- A_HOUSE / B_HOUSE
- ORB

${SORA_AI_PUBLIC_IG_COMMON}
${POLITE_TONE_COMMON}
`.trim();

module.exports = Object.freeze({
  SORA_AI_USER_GUIDE_IG_RESONANCE_SPLIT,
  SORA_AI_USER_GUIDE_IG_RESONANCE_CAPTION,
});
