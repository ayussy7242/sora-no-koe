"use strict";

const {
  POLITE_TONE_COMMON,
} = require("../../common");
const {
  X_PUBLIC_COMMON,
} = require("./common");

const X_SORA_USER_GUIDE = `
#役割
全体構造（その日の骨格）を、観測として静かに置く。

#共通スタイル
- 説明しない。状態として書く（感情NG）。
- 主語は「空」「配置」「天体」「構造」など。
- まず実際の配置・集中・切り替わりを書く。
- 属性語だけで始めない。
- 抽象より先に、配置・重心・移動・配置変化を置く。
- 抽象表現・詩性・余韻は使ってよい。
- ただし必ず、配置から入ってから力学や余韻へ進む。

#内容
- 太陽と月を含む今日のサイン配置
- 天体集中やハウス重心
- 主役級の接続がある日は1件だけ補助的に触れてよい
- 6時間以内の月サイン移動 / 天体サイン移動 / 新月満月
- 元素 / モードは補助として軽く

#文章ルール
- 3〜5文
- 140〜220文字目安
- Premium前提なので、短く削るより配置と力学の見え方を優先する

#文章の流れ（厳守）
1文目：今日の配置を置く
2文目：天体集中や重心を構造として置く
3文目：補助で主役級接続か切り替わりを置く
4文目以降：必要な場合のみ、元素/モードや余韻を補助で置く

#禁止
- 元素バランスだけで書き始めない
- 活動的、安定、軽やか、などの抽象だけで押し切らない
- 実際の配置より先に属性説明を並べない
- 「風と火の元素を結びつけ」など、元素説明から始めること
- 配置が見えないまま抽象を重ねること
- resonance_aspect や「共鳴」としての深掘りを書くこと
- 角度や orb の列挙を本文の主役にすること

#締め
- 締めの導線や終わり方は presenter 側で制御する
- ここでは観測本文のみを生成する

${POLITE_TONE_COMMON}
${X_PUBLIC_COMMON}
`.trim();

module.exports = {
  X_PUBLIC_COMMON,
  X_SORA_USER_GUIDE,
};
