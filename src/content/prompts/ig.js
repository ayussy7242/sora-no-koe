"use strict";

const {
  SORA_AI_PUBLIC_BASE,
  SORA_AI_PUBLIC_POLITE_TONE,
} = require("./common");

/* =================================================
IG｜今日の共鳴（caption用）
================================================= */

const SORA_AI_USER_GUIDE_IG_RESONANCE = `
Instagramキャプションの「今日の共鳴」本文を生成する。
出力は本文のみ。JSON不要。見出し・箇条書き・メタ説明は不要。

形式:
- 4文固定
- 2段落固定（段落間の改行は1回だけ）
- 120〜140字目安（最大150字）
- 1文ごとの改行はしない

流れ:
1) 今日の角度の性質（力学 / 温度 / 圧）
2) 感情の動き
3) 状況の動きや対立
4) バランスと観測ポイント
5) 静かな締め

書き方:
- 断片列挙にせず、各文を自然に閉じる
- 同じ意味を重ねすぎない
- 語尾を単調にしない
- やわらかく、構造が先に見える書き方にする
- 入力データをもとに意味化して書く（羅列しない）

INPUTの使い方:
- ASPECT: 角度の性質の核
- A_HOUSE / B_HOUSE: 共鳴している天体のハウス（whole sign）

${SORA_AI_PUBLIC_POLITE_TONE}
${SORA_AI_PUBLIC_BASE}
`.trim();

/* =================================================
IG｜観測文（slide1）
================================================= */

const SORA_AI_USER_GUIDE_IG_OBSERVATION = `
Instagramカルーセル1枚目の観測文を1文だけ生成する。
出力は本文のみ。JSON不要。句点・改行・箇条書きは不要。

形式:
- 1文のみ
- 18〜28文字目安（最大32文字）
- 「〜配置」または体言止めで閉じる

役割:
- 今日の空の重心をひと目で見せる
- 解釈ではなく、観測の入口として書く
- 入力データをもとに意味化して書く（羅列しない）

優先順位:
1) 星座集中
2) 元素バランス
3) ハウス集中
4) どれも無い場合

テンプレ:
A. ◯◯座付近に天体が多い配置
B. ◯◯・◯◯付近に天体が多い配置
C. ◯と◯のあいだに重なりが見える配置
D. ◯の元素が強い配置
E. 第Nハウス付近に天体が多い配置
F. 天体が全体に散る配置

判断ルール:
- 星座集中: 天体2個以上の星座がある場合にA/Bを使う
- 元素バランス: 2元素が同程度ならC、そうでなければD
- ハウス集中: 最多ハウスでE

INPUTの使い方:
- TRANSIT_SIGNS: 星座集中の判定
- SKY_STRATA.element_count: 元素バランスの判定
- HOUSE_FOCUS.top: ハウス集中の判定

${SORA_AI_PUBLIC_BASE}
`.trim();

/* =================================================
IG｜つきじ構造文（slide4）
================================================= */

const SORA_AI_USER_GUIDE_IG_TSUKIJI_STRUCTURE = `
Instagramカルーセル「つきじ」の構造ラベルを生成する。
出力は本文のみ。JSON不要。改行・箇条書きは不要。

役割:
- 天体名や角度の説明ではなく、合体した意味を短く置く
- イベント説明ではなく、構造の核を書く
- 星座を含んだ天体の質、角度のつながり方、期間の層をまとめて圧縮する

形式:
- 「構造：」で始める
- 1文のみ
- 10〜24文字目安
- 句点なし

書き方:
- 合体した結果の意味を書く
- 説明文にしない
- 天体名・星座名・アスペクト名・日数をそのまま並べない
- 期間の長さは、語感としてにじませる

INPUT:
- A_BODY / B_BODY
- A_SIGN / B_SIGN
- ASPECT / ORB
- START / PEAK / END
- SPAN_DAYS

${SORA_AI_PUBLIC_BASE}
`.trim();

/* =================================================
IG｜今日の月（slide）
================================================= */

const SORA_AI_USER_GUIDE_IG_MOON = `
Instagramカルーセル「今日の月」本文を生成する。
出力は本文のみ。JSON不要。改行・箇条書き・メタ説明は不要。

形式:
- 2文固定
- 1文ごとの改行はしない

内容:
- 月の星座と配置の性質を書く
- 観測ログとして静かに書く
- 入力データをもとに意味化して書く（羅列しない）
- 数値は書かない（月齢/照度/時刻/次の月相は本文に出さない）
- 次の月相や新月/満月の説明はしない（「今日の月」だけを書く）
- 「新月」「満月」は、今日が新月/満月のときだけ本文に出してよい

INPUTの使い方:
- MOON_SIGN: 月のサイン
- PHASE_LABEL: 月相ラベル

${SORA_AI_PUBLIC_POLITE_TONE}
${SORA_AI_PUBLIC_BASE}
`.trim();

module.exports = Object.freeze({
  SORA_AI_USER_GUIDE_IG_RESONANCE,
  SORA_AI_USER_GUIDE_IG_OBSERVATION,
  SORA_AI_USER_GUIDE_IG_TSUKIJI_STRUCTURE,
  SORA_AI_USER_GUIDE_IG_MOON,
});
